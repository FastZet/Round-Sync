<?xml version="1.0" encoding="utf-8"?>
<!--
    Grid cell layout for the file explorer.
    The thumbnail fills a square area at the top (aspect ratio 1:1),
    and the filename sits below it. No details (size/date) or options button.
    Selection state is shown via selectionOverlay.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="?selectableItemBackground">

    <!-- Square thumbnail area using ConstraintLayout aspect-ratio -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:id="@+id/grid_thumbnail_frame">

        <!--
            We rely on a width=match_parent + a post-layout measurement in the adapter
            to set height = width. Alternatively we use a ConstraintLayout workaround.
        -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!-- File thumbnail (images/videos) -->
            <ImageView
                android:id="@+id/file_icon"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_file"
                android:contentDescription="@string/file_icon"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintDimensionRatio="1:1"/>

            <!-- Folder / dir icon -->
            <ImageView
                android:id="@+id/dir_icon"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:scaleType="centerInside"
                android:padding="12dp"
                android:src="@drawable/ic_folder"
                android:contentDescription="@string/file_icon"
                android:visibility="gone"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintDimensionRatio="1:1"/>

            <!-- Selection highlight overlay -->
            <View
                android:id="@+id/selection_overlay"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:background="#66000000"
                android:visibility="gone"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintDimensionRatio="1:1"/>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </FrameLayout>

    <!-- Filename label below thumbnail -->
    <TextView
        android:id="@+id/file_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="2"
        android:ellipsize="end"
        android:textSize="11sp"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:paddingTop="3dp"
        android:paddingBottom="3dp"
        android:gravity="center_horizontal"
        android:fontFamily="sans-serif"/>

</LinearLayout>
